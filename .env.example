# 可选覆盖：应用默认内置了哔卟哔卟共享 Supabase 项目，开箱即用，无需配置。
# 只有当你想指向其他 Supabase 项目时才填写；留空即可使用内置数据库。
# Only public browser credentials. NEVER put a service_role or secret key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# ---- 个推推送（APK 构建时注入 GETUI_APPID 到 AndroidManifest）----
# 从个推开发者中心「应用管理 → 应用信息」复制。客户端只放 App Id；
# App Key / Master Secret 只进 Supabase Edge Function 环境变量，绝不入代码。
GETUI_APP_ID=

# ---- 微信快捷登录（仅服务端：Supabase Edge Function Secrets）----
# BIBU 独立自建微信登录服务，不再依赖 Supabase Custom OAuth Provider。
# 这些变量不属于 Vite 前端，不会被打进浏览器 bundle；也绝对不要把真实值写进任何前端文件或 Git。
# 配置方式：supabase secrets set 或 Dashboard → Edge Functions → Secrets。
# 详细步骤见 docs/wechat-web-login.md。
#
# WECHAT_APP_ID=              # 微信开放平台「网站应用」AppID
# WECHAT_APP_SECRET=          # 微信开放平台「网站应用」AppSecret（严禁提交到 Git）
# WECHAT_MP_APP_ID=           # 可选：公众号 AppID，用于微信内浏览器登录（需同一开放平台账号）